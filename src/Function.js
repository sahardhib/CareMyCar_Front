import React,{ Component } from 'react';
 
function Function (prop) {
    return <div><h1>{ prop.text}</h1></div>
    }

    function test (prop) {
        return <div><h1>{ prop.text}</h1></div>
        }
// This is a component that will display information on the main landing page of our website. It
 export default Function;
